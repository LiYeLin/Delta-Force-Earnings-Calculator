spring.application.name=lcsjz
# ===================================================================
# POSTGRESQL (NeonDB) DATASOURCE CONFIGURATION a
# ===================================================================
spring.datasource.url=jdbc:postgresql://ep-bitter-union-adji9f81-pooler.c-2.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_hF0glwEkR1Sj
spring.datasource.driver-class-name=org.postgresql.Driver

# ===================================================================
# MYBATIS CONFIGURATION (新的配置)
# ===================================================================

# 指定Mapper接口所在的包路径，MyBatis会自动扫描
# mybatis.mapper-locations=classpath:mapper/*.xml


mybatis-plus.global-config.db-config.schema=market_data

# 开启驼峰命名自动转换，例如数据库的 a_column 会自动映射到Java的 aColumn 属性
mybatis.configuration.map-underscore-to-camel-case=true
# 禁用 Spring Boot 对 Docker Compose 的自动集成功能
spring.docker.compose.enabled=false